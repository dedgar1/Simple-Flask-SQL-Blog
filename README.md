# Simple-Flask-SQL-Blog
Flask and SQL blog with minimal no Domain Driven design

# Running the app

- Install dependies with `pipenv install`
- Run a pipenv shell with `pipenv shell`
- Run the app with `python app.py`